#pragma once

#include "Basics.hpp"
#include "Renderer.hpp"

//struct Layer {
//	Time time;
//	Renderer* rendererPtr = nullptr;
//
//	void create();
//	void render();
//	void destroy();
//	void initialize(Renderer& renderer, void(*createFunction)(Layer& layer), void(*renderFunction)(Layer& layer), void(*destroyFunction)(Layer& layer));
//
//private:
//	Bool initialized = false;
//	void (*createFunctionPtr)(Layer& layer) = nullptr;
//	void (*renderFunctionPtr)(Layer& layer) = nullptr;
//	void (*destroyFunctionPtr)(Layer& layer) = nullptr;
//};
